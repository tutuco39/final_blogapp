<div class="container">

  <% @articles.each do |article| %>
    <div class="article_content">
      <%= article.content %>
    </div>
  <% end %>

  <%= link_to 'Profile', profile_path, class: 'btn-secondary' %>
  <%= link_to 'Sign out', destroy_user_session_path, turbo_method: :delete, class: 'btn-secondary' %>


</div>

